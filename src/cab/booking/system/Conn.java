package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
package cab.booking.system;

import java.sql.*;  

public class Conn {
    Connection c;
    Statement s;

    public Conn() {  
        try {  
            // Load the MySQL driver (updated class name)
            Class.forName("com.mysql.cj.jdbc.Driver");  
            
            // Connect to the database cbs1 with user cbs1 and password 123456
            c = DriverManager.getConnection("jdbc:mysql:///cbs1", "cbs1", "123456"); 
            
            s = c.createStatement();  
        } catch(Exception e) { 
            System.out.println(e);
        }  
    }  
}
